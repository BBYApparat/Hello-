Execute the sql file in your database

items for ox_inventory

	["spraycan"] = {
		label = "Spray Can",
		weight = 200,
		stack = true,
		close = true,
		description = "",
	},
	
	["sprayremover"] = {
		label = "Cleaning Kit",
		weight = 200,
		stack = true,
		close = true,
		description = "",
	},

to add those items in qb inventory you need to adapt

------------Add new gangs to script---------------
to add more gangs you need to check information in config files and add a new line in gangs_reputation sql with the gang you want